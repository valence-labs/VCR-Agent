from typing import Literal, Bool
import numpy as np
from sklearn.metrics.pairwise import cosine_similarity


def get_phenoprint(image):
    NotImplemented


def PhenotypePlausibilility(base_state, perturbed_state, predicted_effect) -> Bool:
    """checking if output phenotype is plausible given perturbation or given chain of events"""

    # Use similarity inferecen between query phenotype and known phenotype of pertubations
    # query phenotype -> similar phenotypes -> phenoprint similarity

    # use KG. 
    # whether such pheno effect 
    # genetic perturbation -> protein -> pathways -> functional response -> phenotype





    return 


def PhenomSimilarity(phenoprint_in, phenoprint_out)->float:

    # changes of phenoprint
    pheno_similarity = PhenomSimilarity(phenoprint_in, phenoprint_out)

    
    # changes of gene expression 
    return 


